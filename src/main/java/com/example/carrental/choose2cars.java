package com.example.carrental;

import javafx.event.ActionEvent;
import javafx.scene.control.Button;

public class choose2cars{
   public void ViewButtonClicked(ActionEvent event) {
        /*int counter =0;
        Button clickedButton = (Button) event.getSource();
        int carIndex = Integer.parseInt(clickedButton.getId().substring(4));
        counter++;
        if (counter==1){

            Button clickedButton2 = (Button) event.getSource();
            int carIndex2 = Integer.parseInt(clickedButton.getId().substring(4));

        }*/
    }
}
